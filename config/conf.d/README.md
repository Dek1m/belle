# conf.d

Additional configuration files for belle modules.

Place `*.conf` files here — they will be read by modules
that support conf.d loading (planned for future versions).

Format: KEY=VALUE, one per line.
